import React from "react";
import './style.css'

function Button() {
  /*
  function handleSubmit(e) {
    e.preventDefault();
    console.log('Click');
  }
  */
  return(
      <button type='submit'> 
        Send!
      </button>
    );
  
}

export default Button;