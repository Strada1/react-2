const todoBlocks = [
    { priority: 'high', title: 'HIGH' },
    { priority: 'low', title: 'LOW' },
];

const defaultValue = '';
const tasks = 'tasks';
const isDone = false;

const PRIORITY = {
    HIGH: 'high',
    LOW: 'low',
};

export { defaultValue, todoBlocks, tasks, isDone, PRIORITY };
